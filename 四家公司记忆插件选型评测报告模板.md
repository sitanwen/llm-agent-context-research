# 四家公司记忆插件选型评测报告模板

> 适用范围：在同一套 OpenClaw / openclaw-eval 环境下，对四个候选记忆插件进行横向评测。  
> 使用数据集：LoCoMo、LongMemEval、BEAM、Memora。  
> 本文件中的 `<填写>`、`插件A～D` 均为占位符，不代表真实测试结论。

---

## 0. 使用说明

这份模板采用“能力画像 + 场景选型”的方式呈现结果，不建议只给出一个总分并据此宣布某个插件全面领先。

填写时遵循以下原则：

1. 四个插件必须使用相同的数据、Answer Model、Judge Model、Prompt、Top-K、并发、超时和重试策略。
2. 保留四个数据集的原始分数，同时再映射为统一能力分数。
3. “没有覆盖”记为 `—`，不能按 0 分处理。
4. LoCoMo、LongMemEval、BEAM、Memora 的原始指标含义不同，不得把原始数字直接混在一起求平均。
5. 雷达图中的综合分属于本项目的“内部统一能力指数”，不等同于任何数据集的官方成绩。
6. 结论必须结合样本量和运行失败率；小样本下的分数差异只能作为方向性信号。

---

## 1. 报告摘要

### 1.1 测试结论摘要

> 本次在 `<测试日期>`，使用 `<OpenClaw版本>`、`<openclaw-eval版本>`、`<Answer Model>` 和 `<Judge Model>`，对四个候选记忆插件进行了统一评测。测试覆盖基础召回、跨会话理解、时序理解、记忆更新、记忆遗忘、个性化决策、行为与安全边界七项能力。
>
> 结果显示，四个插件没有形成所有能力维度上的绝对领先：`<插件名称>` 在 `<能力>` 上表现相对更好；`<插件名称>` 在 `<能力>` 上更稳定；`<插件名称>` 的优势主要体现在 `<场景>`；`<插件名称>` 在 `<成本/延迟/部署>` 上更具工程优势。最终选择应根据目标业务对不同能力的权重确定，而不应只依据单一总分。

### 1.2 一页式选型结论

| 候选插件 | 相对优势 | 主要限制 | 更适合的场景 | 结论置信度 |
|---|---|---|---|---|
| 插件A：`<填写>` | `<填写>` | `<填写>` | `<填写>` | `<高/中/低>` |
| 插件B：`<填写>` | `<填写>` | `<填写>` | `<填写>` | `<高/中/低>` |
| 插件C：`<填写>` | `<填写>` | `<填写>` | `<填写>` | `<高/中/低>` |
| 插件D：`<填写>` | `<填写>` | `<填写>` | `<填写>` | `<高/中/低>` |

---

## 2. 评测目标与候选方案

### 2.1 选型目标

本次选型希望回答以下问题：

1. 哪个插件能够稳定召回应当记住的历史事实？
2. 哪个插件更擅长跨会话整合和复杂推理？
3. 哪个插件更擅长时间关系与事件顺序判断？
4. 用户信息发生修改后，哪个插件能够使用最新值？
5. 信息被删除、替换或失效后，哪个插件能避免旧信息残留？
6. 哪个插件更适合长期个性化和推荐任务？
7. 哪个插件在无证据回答、历史指令执行方面更可靠？
8. 在能力接近时，哪个插件具有更好的成本、延迟、稳定性和部署可维护性？

### 2.2 候选插件

| 代号 | 公司 | 插件/版本 | 部署方式 | 备注 |
|---|---|---|---|---|
| A | `<公司名称>` | `<插件名称及版本>` | `<本地/SaaS/混合>` | `<填写>` |
| B | `<公司名称>` | `<插件名称及版本>` | `<本地/SaaS/混合>` | `<填写>` |
| C | `<公司名称>` | `<插件名称及版本>` | `<本地/SaaS/混合>` | `<填写>` |
| D | `<公司名称>` | `<插件名称及版本>` | `<本地/SaaS/混合>` | `<填写>` |

---

## 3. 实验范围与公平性说明

### 3.1 当前实际测试范围

| 数据集 | 官方规模/结构 | 本次实际范围 | 本次题数 | 覆盖性质 |
|---|---|---|---:|---|
| LoCoMo | 公开版10段超长对话，含多类QA | `conversation0` | `<填写>` | 单对话切片 |
| LongMemEval | 500道高质量问题 | 固定抽样120题 | 120 | 分层抽样/固定抽样，需填写方式 |
| BEAM | 100段对话、2,000道问题，最长可到10M tokens | `<100K 3-case / 100K 5-case>` | `<60/100>` | 固定领域切片 |
| Memora | 10种persona，weekly/monthly/quarterly三种时间跨度 | Weekly 3-persona | 45 | 固定persona切片 |

### 3.2 必须写入报告的限制

- LoCoMo 只测试 `conversation0`，不能代表公开版全部10段对话。
- LongMemEval 为120题抽样结果，必须保存题目ID列表，避免不同插件使用不同样本。
- BEAM 3-case/5-case是领域抽样，但每个case内部覆盖全部10种题型；case不是能力类别。
- Memora 3-case实际是3个persona，共466个历史session和45道主问题；persona不是能力类别。
- LLM-as-Judge 可能存在随机性。正式报告应固定Judge版本和温度，并保存逐题Judge解释。
- 如果四个插件的完成题数不同，不能只比较已完成题目的平均分，还要同时报告失败率。

因此，本次结论定位为：

> **面向当前业务和固定样本的工程选型依据，而不是面向整个行业的绝对排行榜。**

---

## 4. 四个测试集基本介绍

## 4.1 LoCoMo

LoCoMo（Long-term Conversational Memory）主要评估模型或记忆系统对超长对话的记忆与问答能力。公开数据包含10段长对话，并提供问题、参考答案和证据对话ID。

主要题型包括：

| 语义题型 | 主要测试内容 | 统一能力 |
|---|---|---|
| Single-hop | 从单个历史片段直接找出事实 | 基础召回 |
| Multi-hop | 联合多个历史片段得出答案 | 跨会话理解 |
| Temporal | 判断时间、持续时间或先后关系 | 时序理解 |
| Open-domain | 结合对话信息与常识作答 | 跨会话理解（需单独标注常识因素） |
| Adversarial | 对话中没有答案时能否避免编造 | 行为与安全边界 |

注意：LoCoMo公开JSON中的数字 `category` 与部分第三方代码中的标签映射长期存在混用。正式统计应优先使用经过核验的语义题型，不应仅凭数字ID命名能力。本报告建议记录：`category 1=Multi-hop`、`2=Temporal`、`3=Open-domain`、`4=Single-hop`、`5=Adversarial`，同时核对你当前runner的实际映射。

本次只运行 `conversation0`，其价值是观察单个长期关系中的事实、跨会话和时间记忆；局限是主题和人物覆盖不足。

## 4.2 LongMemEval

LongMemEval评估聊天助手在长期、多Session历史中的端到端记忆能力。官方数据包含500道问题，并提供不同长度的历史版本以及Oracle证据版本。

主要题型包括：

| `question_type` | 主要测试内容 | 统一能力 |
|---|---|---|
| `single-session-user` | 召回用户在某次会话明确提供的信息 | 基础召回 |
| `single-session-assistant` | 召回助手过去给出的信息 | 基础召回 |
| `single-session-preference` | 使用用户历史偏好回答 | 个性化决策 |
| `multi-session` | 联合多个Session的信息 | 跨会话理解 |
| `temporal-reasoning` | 日期、顺序、持续时间和相对时间推理 | 时序理解 |
| `knowledge-update` | 信息变化后使用最新值 | 记忆更新 |
| `*_abs` | 问题无充分证据时识别为不可回答 | 行为与安全边界 |

LongMemEval能测到“更新后答对新值”，但官方评分允许回答中同时提及部分旧信息，只要最新答案正确。因此它对“旧信息必须完全消失”的要求弱于Memora的 `forgetting_absence`，不能把 LongMemEval `knowledge-update` 直接等同于严格遗忘能力。

## 4.3 BEAM

BEAM面向超长、连贯对话构造记忆探针，覆盖从约100K/128K到10M tokens的不同长度档位。其价值不仅是测“能否找到事实”，还测试冲突、顺序、长期指令和摘要等更接近Agent运行的问题。

BEAM的10种题型如下：

| `question_type` | 主要测试内容 | 统一能力 |
|---|---|---|
| `information_extraction` | 提取明确历史事实 | 基础召回 |
| `multi_session_reasoning` | 跨Session联合推理 | 跨会话理解 |
| `summarization` | 保留关键事实并压缩长历史 | 跨会话理解 |
| `temporal_reasoning` | 时间计算、日期与时间约束 | 时序理解 |
| `event_ordering` | 恢复多个事件的先后次序 | 时序理解 |
| `knowledge_update` | 新值出现后采用最新状态 | 记忆更新 |
| `contradiction_resolution` | 识别并处理相互冲突的信息 | 记忆更新；旧值抑制可辅助观察遗忘 |
| `preference_following` | 使用当前有效偏好 | 个性化决策 |
| `instruction_following` | 长期保持并执行历史指令 | 行为与安全边界 |
| `abstention` | 无证据时拒绝编造 | 行为与安全边界 |

你当前的适配仓库提供两种固定配置：

- 5-case：索引 `0、4、8、12、16`，100道题，每种题型10题；
- 3-case：索引 `0、8、16`，60道题，每种题型6题。

每个case都是一个领域切片，并且覆盖全部10种题型。因此正式能力结论必须按 `question_type` 汇总，不能把Coding、Lifestyle等case名称当作能力名称。

## 4.4 Memora

Memora专门评估个性化Agent在数周到数月持续交互中的长期记忆，强调两件同等重要的事：

1. 应当保留的信息能否被正确使用；
2. 已删除、替换或失效的信息能否不再影响回答。

Memora有三类主任务：

| `question_type` | 主要测试内容 | 统一能力 |
|---|---|---|
| `Remembering` | 召回仍然有效的事实、事件、任务和状态 | 基础召回 |
| `Reasoning` | 综合多条长期记忆得出结论 | 跨会话理解 |
| `Recommending` | 根据当前有效偏好和约束提出建议 | 个性化决策 |

每道主问题下还包含两类更细的判定项：

| `evaluation_type` | 含义 | 统一能力 |
|---|---|---|
| `memory_presence` | 应记住的信息是否被正确体现 | 基础召回/推理/个性化任务的正向正确性 |
| `forgetting_absence` | 已删除、被替换或失效的信息是否没有残留 | 记忆遗忘 |

你当前的Weekly 3-persona配置为：

| Persona | Session数 | Remembering | Reasoning | Recommending | 主问题数 |
|---|---:|---:|---:|---:|---:|
| `software_engineer` | 163 | 5 | 5 | 5 | 15 |
| `financial_analyst` | 156 | 5 | 5 | 5 | 15 |
| `creative_designer` | 147 | 5 | 5 | 5 | 15 |
| 合计 | 466 | 15 | 15 | 15 | 45 |

转换结果共有148个 `memory_presence` 判定项和73个 `forgetting_absence` 判定项。这里的466是历史Session数量，不是466道测试问题。

Weekly、Monthly、Quarterly表示记忆时间跨度与历史规模，不是新的能力类别。如果未来补测Monthly或Quarterly，应作为“记忆耐久度/规模压力”切片单独展示。

---

## 5. 统一能力边界

为突出更新与遗忘的差异，本报告不把它们合并成一个宽泛的“记忆治理”，而是采用七个能力轴。

| 编号 | 统一能力 | 判断问题 | 明确不包含 |
|---:|---|---|---|
| C1 | 基础召回 | 应记住的明确事实能否被找到并正确回答？ | 不包含多证据推理 |
| C2 | 跨会话理解 | 能否组合多条记忆、完成推理或高保真摘要？ | 不包含纯时间计算 |
| C3 | 时序理解 | 能否正确处理日期、先后、持续时间和事件顺序？ | 不包含一般多跳推理 |
| C4 | 记忆更新 | 新旧状态变化后，能否识别当前有效值？ | 不等同于彻底删除旧信息 |
| C5 | 记忆遗忘 | 已删除、替换或失效的信息是否不再影响回答？ | 不包含单纯没有召回到信息 |
| C6 | 个性化决策 | 能否依据当前有效偏好进行推荐和调整回答？ | 不包含通用推荐质量 |
| C7 | 行为与安全边界 | 无证据时能否拒答，历史规则能否持续执行？ | 不等同于完整安全合规评测 |

### 5.1 “没记住”与“忘得对”的区别

- **没记住**：有效信息本应出现，但没有被召回，属于C1失败。
- **忘得对**：旧信息已经失效，回答中没有再使用它，属于C5成功。
- **错误遗忘**：仍然有效的信息被丢失，表现为C1下降。
- **旧值残留**：新值答对了，但仍把旧值当作当前信息，C4可能部分成功、C5失败。

这也是必须同时报告 MPA、FAA 和 FAMA 的原因：只看普通准确率无法区分“记不住”和“不会忘”。

---

## 6. 数据集到统一能力的覆盖矩阵

符号说明：`◎` 直接、主要覆盖；`○` 间接或辅助覆盖；`—` 基本不覆盖。

| 数据集 | C1召回 | C2跨会话 | C3时序 | C4更新 | C5遗忘 | C6个性化 | C7边界 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| LoCoMo | ◎ | ◎ | ◎ | — | — | ○ | ◎ |
| LongMemEval | ◎ | ◎ | ◎ | ◎ | ○ | ◎ | ◎ |
| BEAM | ◎ | ◎ | ◎ | ◎ | ○ | ◎ | ◎ |
| Memora | ◎ | ◎ | ○ | ○ | ◎ | ◎ | — |

解释：

- LoCoMo和LongMemEval提供传统长期对话记忆的核心基线。
- BEAM补强冲突、事件排序、长期指令与超长上下文压力。
- Memora把遗忘从隐含现象变成显式评分对象，是C5的主要数据来源。
- Memora的更新能力只有在能够根据操作/题目元数据识别“replace/update”样本时才能单独统计；否则只报告FAA，不把它包装成纯更新分数。
- LongMemEval与BEAM中“没有使用旧值”只能视为遗忘的辅助信号，因为其评分目标并不完全等同于Memora的 `forgetting_absence`。

---

## 7. 四个数据集的原始计算公式

## 7.1 LoCoMo：Token F1

对普通单答案题，设模型答案Token集合为预测序列 \(P\)，参考答案Token序列为 \(G\)，二者重合Token数量为 \(M\)：

```text
Precision = M / |P|
Recall    = M / |G|
F1        = 2 × Precision × Recall / (Precision + Recall)
```

官方实现会先进行小写化、去标点、去冠词、空格归一化和词干化。

对于Multi-hop多答案题，参考答案和预测答案会按逗号拆分。对每个参考子答案，取它与所有预测子答案之间的最大F1，再对全部参考子答案求平均：

```text
MultiHopF1(q) = mean_g( max_p( F1(p, g) ) )
```

Adversarial题通常使用二元判定：回答表达“没有可用信息/对话未提及”记为1，否则为0。

分题型与总体分数：

```text
CategoryScore(c) = mean(F1_q), q属于题型c
OverallLoCoMo    = mean(F1_q), q属于全部已完成题目
```

建议同时报告：题数、完成率、各题型F1和Overall F1。由于本次只测 `conversation0`，不要与公开全量榜单直接对比。

## 7.2 LongMemEval：LLM-as-Judge Accuracy

官方评测由Judge依据题目类型，对每道答案返回 `yes/no`：

```text
Correct_q = 1，Judge判定答案正确
Correct_q = 0，Judge判定答案错误
```

各题型准确率：

```text
Accuracy(c) = Σ Correct_q / N_c
```

官方脚本同时给出两种总体口径：

```text
Overall Accuracy      = 所有题目的Correct_q平均值（micro）
Task-averaged Accuracy = 各question_type Accuracy的平均值（macro）
Abstention Accuracy   = question_id带_abs题目的Correct_q平均值
```

建议把 `Task-averaged Accuracy` 作为跨能力比较的主口径，把 `Overall Accuracy` 作为总体完成效果，避免题型数量不均衡导致大类支配总分。

## 7.3 BEAM：Rubric Score

根据你的适配仓库，每道题包含多个rubric criterion。Judge对每项给出：

```text
1.0 = 完全满足
0.5 = 部分满足
0.0 = 不满足
```

单题得分：

```text
RubricScore(q) = Σ CriterionScore(q, j) / K_q
```

例如一题有三个criterion，分数为 `[1, 0.5, 0]`：

```text
RubricScore = (1 + 0.5 + 0) / 3 = 0.5
```

题型得分和总体得分：

```text
TypeScore(t)    = mean(RubricScore_q), q属于question_type t
OverallBEAM     = mean(RubricScore_q), q属于全部有效题目
BinaryPass(q)   = 1，当RubricScore(q) >= τ；否则为0
BinaryPassRate  = Σ BinaryPass(q) / N
```

你的 `beam-openclaw-adapter` 默认 `τ=0.5`。报告必须声明阈值，且应以连续的 `mean_rubric_score` 为主、`binary_pass_rate` 为辅助，避免0.49与0.50被解释成完全不同的能力水平。

适配仓库的聚合脚本只负责统计已有的 `rubric_score`，不会自动调用Judge。正式比较时必须保证四个插件使用相同的Judge模型、Prompt和criterion评分规则。

## 7.4 Memora：MPA、FAA与FAMA

对单道主问题 \(q\)：

```text
MPA_q = memory_presence正确项数 / memory_presence总项数
FAA_q = forgetting_absence正确项数 / forgetting_absence总项数
```

如果该题没有 `forgetting_absence`，你的适配脚本令 `FAA_q = 1`。

遗忘惩罚权重：

```text
λ_q = forgetting_absence总项数
      / (memory_presence总项数 + forgetting_absence总项数)
```

逐题FAMA：

```text
FAMA_q = max(0, MPA_q - λ_q × (1 - FAA_q))
```

含义是：

- 正确信息没有被召回，会直接降低MPA；
- 已失效信息仍然出现在回答中，会通过 `(1 - FAA)` 产生额外扣分；
- 遗忘检查越多，惩罚权重越高；
- 分数最低截断到0。

任务/persona/总体FAMA遵循官方Table 3口径：先计算每题FAMA，再求平均并乘100。

```text
BucketFAMA = mean(FAMA_q) × 100
```

你的 `memora-openclaw-adapter` 还会报告聚合后的MPA和FAA：

```text
BucketMPA = 所有memory_presence正确项总数 / 所有memory_presence项总数
BucketFAA = 所有forgetting_absence正确项总数 / 所有forgetting_absence项总数
```

注意：`BucketFAMA` 是逐题FAMA的宏平均，而 `BucketMPA/FAA` 是criterion层面的汇总比例，两者不能相互代入重新计算。

你的适配器默认使用三个Judge对每个criterion进行多数投票：

- `openai/gpt-4.1`
- `anthropic/claude-haiku-4.5`
- `google/gemini-2.5-flash`

单Judge只适合冒烟测试。正式四插件对比应统一使用同一组Judge，或者明确声明改成了哪个Judge配置。

---

## 8. 统一能力分数的计算方法

## 8.1 第一步：保留原始分数

先分别形成四张官方口径结果表，不立即合并：

- LoCoMo：0～1 Token F1，展示时乘100；
- LongMemEval：0～1 Accuracy，展示时乘100；
- BEAM：0～1 Mean Rubric Score，展示时乘100；
- Memora：0～100 FAMA，并同时报告0～1的MPA和FAA。

统一到展示区间：

```text
NormalizedScore = RawScore × 100    （当原始范围为0～1）
NormalizedScore = RawScore          （当原始范围已经为0～100）
```

这只是量纲统一，不代表不同指标的难度完全相同。

## 8.2 第二步：先在每个数据集内部合成能力分

同一个能力对应多个原始题型时，先做题型宏平均，避免题数多的题型自动获得更高权重。

例如BEAM时序能力：

```text
BEAM_时序 = (event_ordering + temporal_reasoning) / 2
```

BEAM更新能力：

```text
BEAM_更新 = (knowledge_update + contradiction_resolution) / 2
```

BEAM跨会话能力：

```text
BEAM_跨会话 = (multi_session_reasoning + summarization) / 2
```

BEAM行为边界：

```text
BEAM_边界 = (instruction_following + abstention) / 2
```

LongMemEval基础召回：

```text
LME_召回 = (single-session-user + single-session-assistant) / 2
```

Memora建议拆开使用：

```text
Memora_召回     = Remembering任务的MPA或FAMA（必须注明选用哪个）
Memora_跨会话   = Reasoning任务FAMA
Memora_个性化   = Recommending任务FAMA
Memora_遗忘     = Overall FAA × 100
```

推荐：雷达图中C1、C2、C6使用任务FAMA，使其同时体现旧信息污染；同时在旁边用MPA/FAA双柱图解释“记得”和“忘得”。如果希望C1只表达纯召回，则使用MPA，但必须全篇保持一致。

## 8.3 第三步：跨数据集合成统一能力指数

对插件 \(p\) 和统一能力 \(c\)：

```text
UnifiedScore(p,c)
  = Σ[Weight(d,c) × DatasetAxisScore(p,d,c)]
    / Σ Weight(d,c)
```

默认建议：覆盖该能力的数据集等权，即 `Weight=1`；间接覆盖的数据集不进入主分，只作为辅助分析。

推荐主分来源：

| 统一能力 | 主分数据来源 |
|---|---|
| C1 基础召回 | LoCoMo Single-hop、LME单Session、BEAM Information Extraction、Memora Remembering |
| C2 跨会话理解 | LoCoMo Multi-hop/Open-domain、LME Multi-session、BEAM Multi-session/Summarization、Memora Reasoning |
| C3 时序理解 | LoCoMo Temporal、LME Temporal、BEAM Temporal/Event Ordering |
| C4 记忆更新 | LME Knowledge Update、BEAM Knowledge Update/Contradiction Resolution |
| C5 记忆遗忘 | Memora FAA为主；LME/BEAM旧值残留率只作辅助 |
| C6 个性化决策 | LME Preference、BEAM Preference Following、Memora Recommending |
| C7 行为与安全边界 | LoCoMo Adversarial、LME Abstention、BEAM Abstention/Instruction Following |

不要把“未覆盖”的数据集记为0分。例如Memora没有独立时序题型，不应因此拉低C3。

## 8.4 可选：业务权重总分

如果管理层需要一个最终选型指数，可以基于明确的业务权重计算，但必须同时保留雷达图：

```text
BusinessFit(p) = Σ BusinessWeight(c) × UnifiedScore(p,c)
Σ BusinessWeight(c) = 1
```

示例权重（仅为模板）：

| 能力 | 知识助手 | 长期陪伴 | 推荐助手 | 流程型Agent | 强合规业务 |
|---|---:|---:|---:|---:|---:|
| 基础召回 | 30% | 15% | 15% | 15% | 15% |
| 跨会话理解 | 25% | 15% | 15% | 20% | 10% |
| 时序理解 | 15% | 10% | 10% | 15% | 15% |
| 记忆更新 | 10% | 15% | 15% | 15% | 20% |
| 记忆遗忘 | 5% | 20% | 15% | 5% | 20% |
| 个性化决策 | 5% | 20% | 25% | 5% | 5% |
| 行为与安全边界 | 10% | 5% | 5% | 25% | 15% |

权重必须由真实业务确认，不能为了让某个插件胜出而事后调整。

---

## 9. 实验配置记录

| 项目 | 配置 |
|---|---|
| 测试日期 | `<填写>` |
| OpenClaw版本/Commit | `<填写>` |
| openclaw-eval版本/Commit | `<填写>` |
| Answer Model | `<填写>` |
| Answer temperature | `<填写>` |
| Judge Model | `<填写>` |
| Judge temperature | `<填写>` |
| Judge Prompt版本/哈希 | `<填写>` |
| LoCoMo样本 | `conversation0，QA数=<填写>` |
| LongMemEval样本 | `固定120题，题目ID文件=<填写>` |
| BEAM样本 | `<100K 3-case/5-case，索引>` |
| Memora样本 | `Weekly 3-persona` |
| Top-K | `<填写>` |
| Embedding Model | `<填写>` |
| Chunk策略 | `<填写>` |
| Ingest批量大小 | `<填写>` |
| 并发数 | `<填写>` |
| Session隔离策略 | `<填写>` |
| 超时/重试策略 | `<填写>` |
| 运行机器 | `<CPU/GPU/内存/地区>` |

每个插件如有无法统一的必要配置，应在下表说明：

| 插件 | 特殊配置 | 无法统一的原因 | 对结果可能造成的影响 |
|---|---|---|---|
| A | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` |

---

## 10. 原始结果表

## 10.1 LoCoMo

| 插件 | 完成/总题数 | Single-hop F1 | Multi-hop F1 | Temporal F1 | Open-domain F1 | Adversarial | Overall F1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| A | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

## 10.2 LongMemEval

| 插件 | 完成/120 | User | Assistant | Preference | Multi-session | Temporal | Update | Abstention | Task-avg | Overall |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

## 10.3 BEAM

| 插件 | 完成/总题数 | Overall Rubric | Pass Rate | Recall | Cross-session | Temporal | Update | Personalization | Boundary |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

BEAM十类原始分数必须另附完整表，不能只保留合并后的六类结果。

## 10.4 Memora

| 插件 | 完成/45 | Overall FAMA | Overall MPA | Overall FAA | Remembering FAMA | Reasoning FAMA | Recommending FAMA |
|---|---:|---:|---:|---:|---:|---:|---:|
| A | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

建议补充MPA与FAA双柱图：MPA高但FAA低，表示“记得住但不会忘”；FAA高但MPA低，可能只是过度保守或大量漏记。

---

## 11. 七维统一能力结果

| 插件 | 基础召回 | 跨会话理解 | 时序理解 | 记忆更新 | 记忆遗忘 | 个性化决策 | 行为与安全边界 |
|---|---:|---:|---:|---:|---:|---:|---:|
| A | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| B | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| C | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| D | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

建议最终至少包含三张图：

1. **七维雷达图**：展示能力形状，不突出单一总冠军。
2. **能力热力图**：方便读取精确差异。
3. **MPA/FAA双柱图**：专门解释记忆与遗忘的平衡。

雷达图下方必须注明：

> 雷达图为基于四个数据集分项结果计算的内部统一能力指数。不同数据集原始指标、难度和样本规模不同，本图用于当前选型中的相对能力画像，不等同于数据集官方总分。

---

## 12. 工程与成本结果

能力结果与工程结果分开呈现，不建议硬合成一个分数。

| 指标 | 插件A | 插件B | 插件C | 插件D |
|---|---:|---:|---:|---:|
| Ingest成功率 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| QA完成率 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| Ingest耗时 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 查询延迟P50 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 查询延迟P95 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 总Input Tokens | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 总Output Tokens | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 单题平均Token | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 总成本 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 超时数 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| Session lock/reset异常 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 索引存储大小 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 部署复杂度 | `<低/中/高>` | `<低/中/高>` | `<低/中/高>` | `<低/中/高>` |
| 可观测性 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

如果某插件通过更多LLM抽取或反思调用换取更高准确率，应明确写为“能力—成本权衡”，而不是只展示准确率。

---

## 13. 分能力分析模板

每项能力建议使用“结果—原因—业务含义—限制”的四句结构。

### 13.1 基础召回

> 在基础召回方面，`<插件>` 在 `<数据集/题型>` 上表现相对较好，说明其对明确历史事实的写入和检索较稳定。`<插件>` 与其差距为 `<填写>`，在当前样本规模下属于 `<接近/小幅差异/明显差异>`。`<插件>` 的主要错误来自 `<漏写入/检索不到/排序靠后/回答阶段遗漏>`。因此，若业务以知识问答和历史事实查询为主，可优先考虑 `<填写>`，但仍需结合成本和延迟。

### 13.2 跨会话理解

> `<插件>` 在Multi-hop、Multi-session、Reasoning和Summarization上的表现更均衡，说明它更容易把分散在多个Session中的证据组合起来。`<插件>` 虽然单事实召回较高，但在多证据问题上出现 `<填写>`，表明检索到单条证据不等于完成跨会话推理。该能力更适合用于项目跟踪、长期任务和复杂用户状态理解。

### 13.3 时序理解

> `<插件>` 在日期计算、事件排序和最新状态判断方面相对稳定；`<插件>` 的错误主要集中在 `<相对日期/持续时间/先后顺序>`。时序能力不仅依赖检索，还依赖是否保留原始时间戳以及检索结果是否按时间组织。对于计划变更、日程跟踪和状态型业务，应优先关注该项，而不是只看总体召回分。

### 13.4 记忆更新

> `<插件>` 更容易在新旧信息同时存在时选择当前有效值，在LongMemEval `knowledge-update` 和BEAM `knowledge_update/contradiction_resolution` 上表现较好。`<插件>` 的典型问题是 `<继续采用旧值/同时输出相互冲突的新旧值/无法判断哪个更新更晚>`。这说明其优势或短板位于记忆写入合并、版本管理或检索排序，而不只是普通召回。

### 13.5 记忆遗忘

> Memora结果显示，`<插件>` 的FAA为 `<填写>`，说明其对已删除、替换或失效内容的抑制能力 `<填写>`。同时其MPA为 `<填写>`：`<若MPA和FAA都高，则说明记忆保留与遗忘较平衡；若FAA高而MPA低，则可能存在过度遗忘；若MPA高而FAA低，则表示旧信息残留明显>`。因此该项应同时参考FAA、MPA和FAMA，不能只引用其中一个数字。

### 13.6 个性化决策

> `<插件>` 在Preference Following和Recommending方面表现更好，说明其能将长期偏好转化为实际回答或推荐约束。需要注意，通用推荐是否“看起来合理”不等于个性化正确；若使用了已经失效的偏好，即使推荐内容本身合理，也应计为记忆错误。

### 13.7 行为与安全边界

> `<插件>` 在无证据问题上更倾向于谨慎拒答，`<插件>` 在长期指令保持方面表现更好。两者体现不同能力：Abstention强调“不编造”，Instruction Following强调“把历史规则转化为当前行为”。高拒答率也可能牺牲有效问题召回，因此应结合基础召回分判断是否过度保守。

---

## 14. 单插件画像模板

### 插件A：`<名称>`

- **能力优势**：`<填写>`
- **能力短板**：`<填写>`
- **记忆—遗忘平衡**：MPA=`<填写>`，FAA=`<填写>`，解释=`<填写>`
- **典型成功样例**：`<填写>`
- **典型失败样例**：`<填写>`
- **成本与性能**：`<填写>`
- **部署与维护**：`<填写>`
- **推荐场景**：`<填写>`
- **不推荐场景**：`<填写>`
- **综合判断**：`<填写>`

> 插件B、C、D复制相同结构填写。

---

## 15. 差异措辞与置信度规则

为了避免小样本结果被写成“断层领先”，建议采用以下内部表述规则：

| 情况 | 推荐措辞 |
|---|---|
| 差异很小或落在置信区间内 | “表现接近”“未观察到稳定差异” |
| 有一定差异，但样本较少 | “当前样本上略有优势”“呈现较好趋势” |
| 多数据集方向一致、重复运行稳定 | “表现相对更好”“优势较稳定” |
| 只在单个题型领先 | “在该题型上表现较好”，不得扩展成总体领先 |
| 得分高但失败率/成本明显更高 | “以更高资源消耗换取能力提升” |

如需设置内部数值阈值，可暂定：小于3个百分点视为接近，3～8个百分点视为小幅优势，大于8个百分点且多次运行一致才称为明显优势。该阈值仅用于报告措辞，不具有统计学普适性。

---

## 16. 场景化选型结论

| 业务场景 | 首要能力 | 推荐插件 | 备选插件 | 选择理由 | 风险 |
|---|---|---|---|---|---|
| 企业知识助手 | 召回、跨会话 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 长期陪伴助手 | 更新、遗忘、个性化 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 推荐助手 | 个性化、遗忘 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 项目/计划助手 | 时序、更新、跨会话 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 流程执行Agent | 指令、边界、更新 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 医疗/金融等强合规场景 | 遗忘、拒答、更新 | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

最终建议写法：

> 四个插件在不同能力维度上呈现不同取向，没有一个方案在所有指标上形成绝对优势。若业务优先 `<能力/场景>`，建议选择 `<插件>`；若更关注 `<另一能力/工程指标>`，则 `<插件>` 更合适。`<插件>` 可作为 `<备选/低成本/特定场景>` 方案。最终上线前建议围绕真实业务数据补充 `<填写>` 测试，以验证本次公共数据集结论能否迁移到生产环境。

---

## 17. 失败案例归因表

| 数据集 | 题目ID | 插件 | 能力 | 错误类型 | 根因判断 | 证据 |
|---|---|---|---|---|---|---|
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | 未写入 | 记忆抽取遗漏 | `<日志/记录>` |
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | 已写入但未召回 | 检索/排序问题 | `<日志/Top-K>` |
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | 旧值残留 | 更新/遗忘失败 | `<新旧值证据>` |
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | 推理错误 | Answer Model未整合证据 | `<检索内容与答案>` |
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | 超时/锁冲突 | 运行时问题 | `<日志>` |

建议把错误定位到四个阶段：写入、存储/更新、检索、回答。否则容易把Answer Model的推理错误全部归因于记忆插件。

---

## 18. 发布前检查清单

- [ ] 四个插件使用完全相同的样本ID。
- [ ] 记录了Answer Model与Judge Model的精确版本。
- [ ] 保存了每道题的回答、评分、Judge解释和错误日志。
- [ ] LoCoMo使用核验后的语义题型映射。
- [ ] LongMemEval同时报告Task-averaged和Overall Accuracy。
- [ ] BEAM保留十类原始分数，并声明Pass阈值。
- [ ] Memora同时报告MPA、FAA和FAMA。
- [ ] 更新与遗忘在报告中是两个独立能力。
- [ ] 未完成、超时和错误题目没有被静默排除。
- [ ] 雷达图注明其为内部统一能力指数。
- [ ] 工程指标与能力指标分开呈现。
- [ ] 未根据结果事后调整业务权重。
- [ ] 结论没有超出当前抽样范围。

---

## 19. 参考资料与实现口径

1. Agent Memory Leaderboard：<https://agentmemories.ai/leaderboard/industry/textual>
2. LoCoMo官方仓库：<https://github.com/snap-research/locomo>
3. LongMemEval官方仓库：<https://github.com/xiaowu0162/LongMemEval>
4. BEAM论文：<https://openreview.net/pdf/22100aaa38efcf1005b52552c0aafde100515c75.pdf>
5. Memora官方仓库：<https://github.com/geniesinc/Memora>
6. 本项目BEAM适配器：<https://github.com/sitanwen/beam-openclaw-adapter>
7. 本项目Memora适配器：<https://github.com/sitanwen/memora-openclaw-adapter>

本文中的BEAM聚合公式依据 `beam-openclaw-adapter/scripts/aggregate_beam_scores.py`；Memora的Judge与FAMA公式依据 `memora-openclaw-adapter/scripts/judge_memora_answers.py` 和 `scripts/aggregate_fama_scores.py`。

